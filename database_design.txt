Notes:
    - We need to deal with the concept of flagged notes.
    - Attachments TBD
    - Photo handling TBD

Children {
ID : CHAR(16) <pkey>,
ChineseName : VARCHAR(max),
PinyinName : VARCHAR(max),
EnglishName : VARCHAR(max),
Nickname : VARCHAR(max),
Sex : CHAR(1),
DOB : DATE,
ChildPhoto : BLOB,
AbandonmentDate : DATE,
ProgramEntryDate : DATE,
ProgramDepartureDate : DATE,
ProgramDepartureReason : VARCHAR(max),
ChildHistory : VARCHAR(max),
IsActive : BOOL,
MedicalHistory : VARCHAR(max),
}

AssessmentTypes {
ID : CHAR(16) <pkey>,
AssessmentName : VARCHAR(max)
}

ChildPartners {
ChildID : CHAR(16) <fkey>,
PartnerID : CHAR(16) <fkey>,
StartDate : DATE,
EndDate : DATE,
Notes : VARCHAR(max)
}

Partners {
ID : CHAR(16) <pkey>,
PartnerName : VARCHAR(max),
Email : VARCHAR(max),
Address : VARCHAR(max),
Phone : VARCHAR(max),
Notes : VARCHAR(max)
}

ChildNotes {
ChildID : CHAR(16) <fkey>,
NoteDate : DATE,
Note : VARCHAR(max),
}

ChildCamps {
ChildID : CHAR(16) <fkey>,
CampID : CHAR(16) <fkey>,
CampStartDate : DATE,
Notes : VARCHAR(max),
}



Camps {
ID : CHAR(16) <pkey>,
CampName : VARCHAR(max),
}

ChildAssessments {
ChildID : CHAR(16) <fkey>,
AssessmentID : CHAR(16) <fkey>,
AssessmentDate : DATE,
Notes : VARCHAR(max),
}

ChildCaregiverHistory {
ChildID : CHAR(16) <fkey>,
CaregiverID : CHAR(16) <fkey>,
StartDate : DATE,
EndDate : DATE,
Notes : VARCHAR(max),
}

ChildMeasurements {
ChildID : CHAR(16) <fkey>,
MeasurementID : CHAR(16) <fkey>,
MeasurementDate : DATE,
MeasurementValue : FLOAT,
}

ChildMilestones {
ChildID : CHAR(16) <fkey>,
MilestoneID : CHAR(16) <fkey>,
DateReached : DATE,
}

MilestoneTypes {
ID : CHAR(16) <pkey>,
MilestoneName : VARCHAR(max),
MilestoneChineseName : VARCHAR(max),
MilestonePinyinName : VARCHAR(max),
MilestoneCategory : CHAR(16) <fkey>,
}

MilestoneTypeCategories {
ID : CHAR(16) <pkey>,
MilestoneCategoryName : VARCHAR(max),
MilestoneCategoryChinese Name : VARCHAR(max),
MilestoneCategoryPinyin Name : VARCHAR(max),
}

ChildDoctorVisitHistory {
ChildID : CHAR(16) <fkey>,
DoctorID : CHAR(16) <fkey>,
VisitDate : DATE,
Notes : VARCHAR(max),
}

DoctorTypes {
ID : CHAR(16) <pkey>,
DoctorType : VARCHAR(max),
}

Doctors {
ID : CHAR(16) <pkey>,
DoctorType : CHAR(16) <fkey>,
FacilityName : VARCHAR(max),
FacilityChineseName : VARCHAR(max),
FacilityPinyinName : VARCHAR(max),
DoctorName : VARCHAR(max),
DoctorChineseName : VARCHAR(max),
DoctorPinyinName : VARCHAR(max),
}

MeasurementTypes {
ID : CHAR(16) <pkey>,
MeasurementName : VARCHAR(max),
MeasurementChineseName : VARCHAR(max),
MeasurementPinyinName : VARCHAR(max),
MeasurementUnits : VARCHAR(max),
}

Caregivers {
ID : CHAR(16) <pkey>,
ChineseName : VARCHAR(max),
PinyinName : VARCHAR(max),
EnglishName : VARCHAR(max),
}

ChildMedicalConditions {
ChildID : CHAR(16) <fkey>,
ConditionID : CHAR(16) <fkey>,
}

MedicalConditions {
ID : CHAR(16) <pkey>,
ConditionName : VARCHAR(max),
ConditionPinyinName : VARCHAR(max),
ConditionChineseName : VARCHAR(max),
}

ChildMedications {
ChildID : CHAR(16) <fkey>,
MedicationID : CHAR(16) <fkey>,
IsActive : BOOL,
StartDate : DATE,
EndDate : DATE,
Dosage1 : FLOAT,
Dosage2 : FLOAT,
Dosage3 : FLOAT
}

Medications {
ID : CHAR(16) <pkey>,
MedicationName : VARCHAR(max),
MedicationNameChinese : VARCHAR(max),
MedicationNameEnglish : VARCHAR(max),
DoseInmg : FLOAT,
}



